[pytest]
# Integration tests hit the live MSRC API. Keep them out of the default run
# (and the PR critical path); opt in with `pytest -m integration`.
markers =
    integration: tests that make live network calls to the MSRC API
addopts = -m "not integration"
